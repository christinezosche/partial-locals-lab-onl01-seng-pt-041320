<h1>Search for a Student:</h1><br>
<%= form_tag students_path, method: :get do %>
    <%= text_field_tag(:query, params[:query]) %><br>
    <%= submit_tag "Search" %>
<% end %>



<% @students.each do |s| %>
    <%= render partial: "students/student", locals: {student: s} %>
<% end %>

